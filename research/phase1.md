# Pass by Value vs. Pass by reference.

Pass by Value:  is passing a single value to a function. When those values are returned the initial values will not change at all. 

Pass by Reference is sending the adress of the values we want in the function so when they do actually change within the function the initial values will also change.
